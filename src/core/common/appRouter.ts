const PREFIX = "";

export const ROUTE_PATH = {

    LOGIN: `${PREFIX}/login`,
    REGISTER: `${PREFIX}/register`,
    ///Client
    HOME_PAGE: `${PREFIX}/`,
    CUSTOM_CARD: `${PREFIX}/custom-card`,
    LINK: `${PREFIX}/link`,
    PROFILE: `${PREFIX}/profile`,


}